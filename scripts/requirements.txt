# Build-time only — needed to regenerate the raster assets in public/
# (og.png, apple-touch-icon.png, icon-192.png, icon-512.png) via:
#
#   pip install -r scripts/requirements.txt
#   npm run images
#
# The generated PNGs are committed, so a normal `npm install && npm run build`
# never needs Python.
pillow>=10.0
numpy>=1.24
